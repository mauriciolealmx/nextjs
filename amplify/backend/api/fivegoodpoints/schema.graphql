type Reason {
  title: String
  description: String
  votes: Int
  id: ID
}

type Post @model @auth(rules: [{ allow: public, operations: [create, read] }]) {
  id: ID!
  title: String
  content: String
  createdAt: AWSDateTime
  reasons: [Reason]
}
